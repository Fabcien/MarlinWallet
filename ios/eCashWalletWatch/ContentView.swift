import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("eCash Wallet")
            .font(.headline)
            .foregroundColor(.white)
    }
}

#Preview {
    ContentView()
}

